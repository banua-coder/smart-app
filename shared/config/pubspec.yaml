name: config
description: "Config related package for Smart."
version: 0.0.1
publish_to: "none" # Remove this line if you wish to publish to pub.dev

environment:
  sdk: ">=3.2.3 <4.0.0"
  flutter: ">=1.17.0"
  
dependencies:
  envied: ^0.5.3
  flutter:
    sdk: flutter

dev_dependencies:
  build_runner: ^2.4.7
  envied_generator: ^0.5.3
  flutter_lints: ^2.0.0
  flutter_test:
    sdk: flutter
  very_good_analysis: ^5.1.0

